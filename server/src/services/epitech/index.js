const path = require('path');

const metadata = {
  name: path.basename(__dirname),
  displayName: 'Epitech',
  description: 'Uses the Epitech intra API.',
  needsOAuth: false,
};

module.exports = metadata;
